<Window
    x:Class="IndicoPenSpike.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Indico Pen Spike">

    <Grid Background="#F2F2F2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" IsOpen="True" DefaultLabelPosition="Right" Background="#FCFCFC">
            <AppBarButton x:Name="OpenButton" Icon="OpenFile" Label="Open PDF" Click="OpenPdf_Click" />
            <AppBarToggleButton x:Name="InkModeButton" Icon="Edit" Label="Ink" IsChecked="True" />
            <AppBarToggleButton x:Name="EraserModeButton" Icon="Clear" Label="Eraser" />
            <AppBarButton x:Name="UndoButton" Icon="Undo" Label="Undo" Click="Undo_Click" />
            <AppBarButton x:Name="RedoButton" Icon="Redo" Label="Redo" Click="Redo_Click" />

            <CommandBar.Content>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Spacing="20">
                    <TextBlock x:Name="StrokeCountText" Text="0 strokes" VerticalAlignment="Center" />
                    <TextBlock x:Name="PointerStatusText" Text="No pointer input yet" VerticalAlignment="Center" />
                </StackPanel>
            </CommandBar.Content>
        </CommandBar>

        <ScrollViewer x:Name="DocumentScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="#E5E5E5">
            <Grid>
                <StackPanel x:Name="DocumentStack" Padding="32" Spacing="28" HorizontalAlignment="Left" />
                <Grid x:Name="EmptyStateOverlay">
                    <TextBlock
                        Text="PDF Pen Spike&#10;&#10;Open a local PDF, then try pen input, erasing, undo, redo, palm rejection, and scrolling."
                        TextAlignment="Center"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontSize="24"
                        Foreground="#444444"
                        MaxWidth="620"
                        TextWrapping="WrapWholeWords" />
                </Grid>
            </Grid>
        </ScrollViewer>

    </Grid>
</Window>
